//- NAVBAR COMPONENT

#navbar.navbar.navbar-inverse.depth-1
  .collapse.navbar-collapse
    .nav.navbar-nav
      a(href='/')
        img#logo(
          src='/public/imgs/eating.jpg',
          alt='Diabetes Logs',
          )
      - if (breadcrumbs)
        - each crumb in breadcrumbs
          img(src='/public/imgs/breadcrumb-next.svg').crumb-img
          .crumb
            - if (crumb.href)
              a(href= crumb.href)
                = crumb.text
            - else
              = crumb.text
